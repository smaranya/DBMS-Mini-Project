<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Users Data</title>
</head>
<body>
       <h1>Users using this application</h1> 
       <table>
        <tr>
            <th>Name</th>
            <th>Email</th>
            <th>Phone Number</th>
        </tr>

        <% if(result.length > 0) { %>
            <% for( var i = 0; i < result.length; i++ ) { %>
                <tr>
                    <td><%- result[i].FNAME + " "+ result[i].MNAME + " "+ result[i].LNAME%></td>
                    <td><%- result[i].USER_EMAIL %></td>
                    <td><%- result[i].USER_PHONE %></td>
                </tr>
            <% } %>   
        <% }else{ %>
            <h1>User Data not found!</h1>
        <% } %>    
              
       </table>
</body>
</html>